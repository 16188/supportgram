# Supportgram 二开项目部署与运维手册

本文档适用于当前 MAITG Supportgram 二开项目的 VPS 安装、初始化、更新、备份和故障排查。

## 1. 当前部署信息

| 项目 | 当前配置 |
| --- | --- |
| GitHub 仓库 | `https://github.com/16188/supportgram` |
| Docker 镜像 | `ghcr.io/16188/supportgram:latest` |
| VPS 部署目录 | `/www/wwwroot/maitgsupportgram.888866.xyz` |
| 正式域名 | `https://maitgsupportgram.888866.xyz` |
| 容器端口 | `3000` |
| 数据库 | `data/supportgram.db` |
| 图片和视频 | `data/uploads/` |
| 配置文件 | `.env` |

每次代码推送到 GitHub 的 `main` 分支后，GitHub Actions 会自动构建并发布最新版 Docker 镜像。

> **重要：**任何安装、更新和排错操作都不要删除或覆盖 `.env` 和 `data/`。

## 2. 全新 VPS 安装

### 2.1 检查基础环境

```bash
git --version
docker --version
docker compose version
```

如果提示 `docker: 'compose' is not a docker command`，安装 Compose 插件：

```bash
apt-get update
apt-get install -y docker-compose-plugin
docker compose version
```

如果 Docker 本身尚未安装，请参考：

- [Docker Engine 官方安装教程](https://docs.docker.com/engine/install/ubuntu/)
- [Docker Compose 官方安装教程](https://docs.docker.com/compose/install/linux/)

### 2.2 拉取项目文件

宝塔创建的网站目录通常已经包含 `.htaccess`、`.user.ini` 等文件，因此不建议直接在非空目录运行 `git clone ... .`。使用下面的命令拉取项目：

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz

git init
git config --global --add safe.directory /www/wwwroot/maitgsupportgram.888866.xyz

git remote remove origin 2>/dev/null || true
git remote add origin https://github.com/16188/supportgram.git

git fetch origin main
git checkout -B main origin/main
```

检查文件是否完整：

```bash
ls -la
test -f docker-compose.yml && echo "项目文件正常"
```

目录中应当能看到 `docker-compose.yml`、`Dockerfile`、`package.json` 等文件。

### 2.3 配置环境变量

如果 `.env` 已存在，不要覆盖：

```bash
test -f .env || cp .env.example .env
nano .env
```

内容应类似：

```env
TURSO_DATABASE_URL=file:data/supportgram.db
TURSO_AUTH_TOKEN=
BASE_URL=https://maitgsupportgram.888866.xyz
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=
```

### 2.4 创建数据目录

```bash
mkdir -p data
chown -R 1000:1000 data
```

数据最终保存在：

```text
/www/wwwroot/maitgsupportgram.888866.xyz/data/supportgram.db
/www/wwwroot/maitgsupportgram.888866.xyz/data/uploads/
```

### 2.5 启动服务

```bash
docker compose pull
docker compose up -d
docker compose ps
curl http://127.0.0.1:3000/health
```

健康检查正常时会返回：

```json
{"ok":true}
```

## 3. 宝塔反向代理

宝塔网站根目录保持为：

```text
/www/wwwroot/maitgsupportgram.888866.xyz
```

给整个域名配置反向代理，目标地址为：

```text
http://127.0.0.1:3000
```

同时完成以下设置：

- 为域名申请并启用 HTTPS 证书。
- 开启强制 HTTPS。
- 如果使用 Nginx，将上传大小限制调整为 `25m`：

```nginx
client_max_body_size 25m;
```

设置完成后检查：

```bash
curl -I https://maitgsupportgram.888866.xyz/widget.js
curl https://maitgsupportgram.888866.xyz/health
```

## 4. 第一次初始化客服

初始化前准备：

- Telegram Bot Token。
- 已开启“话题”功能的 Telegram 超级群 ID，例如 `-100123456789`。
- 客服人员的 Telegram 数字 ID。
- 需要嵌入客服组件的网站域名。
- 将 Bot 加入超级群，并授予“管理话题”和“删除消息”权限。

执行初始化：

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz

docker compose run --rm supportgram npm run seed -- \
  --name "MAITG客服" \
  --bot-token "替换成Bot Token" \
  --supergroup "-100123456789" \
  --origins "https://maitgsupportgram.888866.xyz,https://你的业务网站域名" \
  --agents "Telegram数字ID:客服名称:Telegram用户名"
```

初始化成功后会输出一个以 `pk_` 开头的公开密钥，例如：

```text
pk_xxxxxxxxx
```

在业务网站中嵌入客服组件：

```html
<script
  src="https://maitgsupportgram.888866.xyz/widget.js"
  data-key="pk_xxxxxxxxx">
</script>
```

初始化通常只执行一次。重复执行可能创建新的客服主体。查看已有客服主体：

```bash
docker compose run --rm supportgram npm run seed -- --list
```

## 5. 日常更新和升级

先确认 GitHub Actions 已经成功构建最新版镜像，然后登录 VPS 执行：

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz

git pull --ff-only
docker compose pull
docker compose up -d --force-recreate
docker compose run --rm supportgram npm run seed -- --sync-telegram

docker compose ps
curl http://127.0.0.1:3000/health
docker compose logs --tail=100 supportgram
```

更新过程中注意：

- 不需要重新执行 `seed`。
- 不要删除 `data/`。
- 不要覆盖 `.env`。
- 不要执行 `docker compose down -v`。
- 数据库结构升级会在容器启动时自动处理。
- `docker compose pull` 不会删除数据库和上传文件。

## 6. 升级前备份

较大更新前建议备份数据：

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz

docker compose stop supportgram
tar -czf "../maitgsupportgram-data-$(date +%Y%m%d-%H%M%S).tar.gz" data
docker compose start supportgram
```

备份文件包含数据库、图片和视频。`.env` 含有敏感配置，可以另外妥善备份，但不要上传到 GitHub 或发送给他人。

## 7. 常用维护命令

查看运行状态：

```bash
docker compose ps
```

查看最近日志：

```bash
docker compose logs --tail=200 supportgram
```

持续查看日志：

```bash
docker compose logs -f supportgram
```

重启服务：

```bash
docker compose restart supportgram
```

查看健康状态：

```bash
curl http://127.0.0.1:3000/health
```

## 8. 常见问题

### 8.1 `docker compose` 不是命令

```bash
apt-get update
apt-get install -y docker-compose-plugin
```

### 8.2 `no configuration file provided`

这通常说明当前目录不对，或者项目文件没有真正拉取下来：

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz
pwd
ls -la
```

如果目录中只有 `.git`、`.env` 等文件，重新执行：

```bash
git fetch origin main
git checkout -B main origin/main
```

### 8.3 `detected dubious ownership`

```bash
git config --global --add safe.directory /www/wwwroot/maitgsupportgram.888866.xyz
```

### 8.4 上传图片或视频提示权限错误

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz
chown -R 1000:1000 data
docker compose restart supportgram
```

### 8.5 上传时出现 `413 Request Entity Too Large`

将 Nginx 上传限制调整为：

```nginx
client_max_body_size 25m;
```

然后重新加载 Nginx。

### 8.6 `/widget.js` 返回 404

```bash
cd /www/wwwroot/maitgsupportgram.888866.xyz
docker compose pull
docker compose up -d --force-recreate
docker compose logs --tail=100 supportgram
```

### 8.7 更新后仍显示旧页面

先按 `Ctrl + F5` 强制刷新浏览器，然后确认 VPS 已拉取并重新创建最新版容器。

## 9. Telegram 客服命令

- 直接编辑自己发出的 Telegram 消息：同步修改访客网页中的客服回复。
- 回复需要撤回的客服消息并发送 `/undo`：删除网页消息、对应媒体文件和 Telegram 原消息。
- `/block`：拉黑当前访客，阻止其继续发送、上传文件或重新创建会话。
- `/unblock`：解除当前访客黑名单，恢复会话。
- `/close`：关闭当前会话。
- `/note 内容`：添加仅客服可见的内部备注。
- `/delete`：显示永久删除警告。
- `/delete confirm`：永久删除当前对话、VPS 中对应的数据库记录和媒体文件，并删除 Telegram 话题。

永久删除不可恢复，执行前应确认不再需要该会话。

## 10. 后续开发提示词

以后需要继续修改项目时，可以直接发送下面这段：

```text
请继续维护下面这个二开项目，并直接完成修改、测试、提交和发布。

GitHub 项目：
https://github.com/16188/supportgram

本地项目目录：
工作区下的“项目1”文件夹

VPS 部署目录：
/www/wwwroot/maitgsupportgram.888866.xyz

正式域名：
https://maitgsupportgram.888866.xyz

本次需求：
【在这里详细写需求，可以附截图】

要求：
1. 先检查现有代码和当前实现，保持已有功能兼容；
2. 数据库继续保存在 data/supportgram.db；
3. 图片和视频继续保存在 data/uploads；
4. 禁止删除、覆盖或提交 VPS 的 data 和 .env；
5. 数据不能自动过期或自动清除；
6. 如果修改数据库结构，需要提供自动迁移，不能丢失旧数据；
7. 同步更新 CHANGELOG.md；
8. 完成必要的测试和构建；
9. 提交并 push 到 GitHub main 分支；
10. 确认 GitHub Actions 镜像构建结果；
11. 最后给我可以直接复制执行的 VPS 无损升级命令和验证方法。
```

如果只是修复故障，可以发送：

```text
Supportgram 项目出现以下问题，请先定位根因，再直接修复：

问题表现：
【填写问题】

复现步骤：
【填写操作过程】

错误日志：
【粘贴 docker compose logs --tail=200 supportgram 的结果】

请保持 data 和 .env 不受影响，修复后更新 CHANGELOG.md、测试、push 到 main，并给我 VPS 升级命令。
```

> 发送日志和截图前，请遮住 Telegram Bot Token、`.env`、GitHub Token、Webhook Secret 等敏感信息。
